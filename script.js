const translations = {
  en: {
    // Navigation
    navServices: "Services",
    navTech: "Technology",
    navProjects: "Projects",
    navContact: "Contact",
    navAbout: "About",
    navExperience: "Experience",
    navHowWeWork: "How We Work",
    navFaq: "FAQ",

    // Hero Section
    heroTitle: "Financial planning that keeps up with your business.",
    heroDesc: "We are a finance consulting company. We work on FP&A, financial modelling, reporting and automation, building the forecast models and the systems that keep them running.",
    btnServices: "See What We Build",
    btnWork: "Work With Us",

    // Services Section
    servicesTitle: "Services",
    colHeader1: "FP&A (Financial Planning & Analysis)",
    colHeader2: "Financial Process Automation",
    colHeader3: "Custom Financial Ecosystem",

    // Services List Items — Card 1: FP&A
    li1: "Driver-Based Forecasting",
    li2: "Rolling Budget Cycles",
    li3: "KPI & Management Reporting",
    li4: "Variance & Scenario Analysis",
    // Card 2: Financial Process Automation
    li5: "AI Agents & Document Processing",
    li6: "MCP & System Integration",
    li7: "Close & Reconciliation Automation",
    li8: "Reporting Pipelines",
    // Card 3: Custom Financial Ecosystem
    li9: "Bespoke Finance Tools",
    li10: "ERP & BI Integration",
    li11: "Data Architecture",
    li12: "Machine Learning Models",

    // Expertise Descriptions
    expDesc1: "A budget agreed in November is usually out of date by March. We build driver-based rolling forecasts and reporting that get updated as the business changes, so the plan still means something later in the year.",
    expDesc2: "In a lot of finance teams, invoice coding, reconciliation and month-end reporting are still done by hand. We connect the systems and automate the repetitive parts, and we use AI agents where something needs to be read or checked.",
    expDesc3: "Sometimes the tool you need does not exist yet. We build it for you: liquidity tools, pricing models, and the data setup behind them, made to fit how your company works.",

    // Tech Section
    techTitle: "Technology",
    techSub1: "Financial Planning & Analytics",
    techSub2: "ERP & Integration Platforms",

    // Projects Section
    projectsTitle: "Projects",
    p1Name: "Head of FP&A, Aviation Start-up",
    p1Desc: "Financial modelling, vendor contract analysis and macroeconomic forecasting (e.g. fuel prices) with SQL and machine learning. Built custom finance tools, including invoice OCR and a liquidity engine, integrated with the existing platforms.",
    p2Name: "Integrated Planning in Anaplan",
    p2Desc: "End-to-end Anaplan implementation for a large global healthcare company. Combined the sales and financial planning cycles into one driver-based model, so sales inputs flow directly into the financial forecast.",
    p3Name: "",
    p3Desc: "",
    p4Name: "",
    p4Desc: "",
    seeStory: "See the story →",

    // Contact Section
    contactSub: "Start a Conversation",
    contactTitle: "Book a Call",
    bookingKicker: "30-MINUTE DISCOVERY CALL",
    bookingHeading: "Let’s scope it together",
    bookingIntro: "A short call to understand the scope — what is not working, what you are trying to build, and whether we are the right fit. No pitch deck, no obligation.",
    bookingBtn: "Show available times",
    bookingFallback: "Calendar not loading? Open it in a new tab →",
    bookingNote: "Opens Microsoft Bookings in a new tab. Nothing is sent to Microsoft until you choose to open it — see our <a href=\"./privacy-policy.html\" target=\"_blank\" rel=\"noopener noreferrer\">Privacy Policy</a>.",

    // Footer
    footer: "Copyright © 2026 PMG Services. All Rights Reserved.",
    footerTagline: "Building finance functions that make better decisions.",
    footerContactHeading: "Contact",
    footerLegalHeading: "Legal",
    footerPrivacyLink: "Privacy Policy",
    footerImpressumLink: "Impressum",

    // About Us Section
    auLabel: "Who we are",
    auHeadline: "You have the data.<br><span class=\"au-headline-accent\">We make it useful.</span>",
    auLead: "If your team has the data but struggles to turn it into useful insights, or spends too much time on manual work, that is when we come in. We build the forecast models, reporting and automation that give you a clear view of where the business is heading.",
    auFounderRole: "Founder & CEO",
    auFounderBio: "I founded PMG Services to close the gap between the data finance teams already have and the decisions they actually have to make. I previously worked in finance transformation at a Big Four firm, and I have led teams of 100+ people on S/4HANA transformation projects in multinational companies. We build the forecast models, reporting and automation that make that data usable, and we build them to keep running without us.",
    founderCta: "Book a discovery call",

    // How We Work Section
    hwwLabel: "Our process",
    hwwTitle: "How We Work",
    hwwIntro: "Three phases, with a clear end date. You know what you get at each stage and what it costs before we start.",
    hwwStep1Title: "Discover",
    hwwStep1Desc: "We start by looking at how your finance team works today: the data, the processes, the tools and the spreadsheets nobody wrote down. Then we send you what we found, before suggesting anything.",
    hwwStep1Bullets: [
      "Process & decision audit",
      "Data quality assessment",
      "Technology landscape review",
      "Stakeholder alignment sessions"
    ],
    hwwStep2Title: "Design",
    hwwStep2Desc: "We design the solution, from the process rules to the technical setup. Every part of it is tied to something you can measure, and we agree the scope before anyone starts building.",
    hwwStep2Bullets: [
      "Solution architecture",
      "KPI & success metrics",
      "Implementation roadmap",
      "Risk & change management"
    ],
    hwwStep3Title: "Deploy",
    hwwStep3Desc: "We build it together with your team and train them as we go, so the work keeps running after we leave.",
    hwwStep3Bullets: [
      "Agile delivery sprints",
      "Integration & testing",
      "Team training & adoption",
      "Hypercare & handover"
    ],

    // Contact direct
    contactDirectIntro: "Prefer a direct conversation? Send us an email or find us on LinkedIn — no pitch decks, just a conversation about what you're trying to solve."
  },
  de: {
    // Navigation
    navServices: "Leistungen",
    navTech: "Technologie",
    navProjects: "Projekte",
    navContact: "Kontakt",
    navAbout: "Über uns",
    navExperience: "Erfahrung",
    navHowWeWork: "Wie wir arbeiten",
    navFaq: "FAQ",

    // Hero Section
    heroTitle: "Finanzplanung, die mit Ihrem Unternehmen Schritt hält.",
    heroDesc: "Wir sind eine Finanzberatung. Wir arbeiten an FP&A, Finanzmodellierung, Reporting und Automatisierung und bauen die Forecast-Modelle und die Systeme, die sie am Laufen halten.",
    btnServices: "Unsere Leistungen",
    btnWork: "Kontaktieren Sie uns",

    // Services Section
    servicesTitle: "Leistungen",
    colHeader1: "FP&A (Finanzplanung & -analyse)",
    colHeader2: "Automatisierung von Finanzprozessen",
    colHeader3: "Individuelles Finanz-Ökosystem",

    // Services List Items — Card 1: FP&A
    li1: "Treiberbasierte Prognose",
    li2: "Rolling-Budget-Zyklen",
    li3: "KPI- & Management-Reporting",
    li4: "Varianz- & Szenarioanalyse",
    // Card 2: Automatisierung von Finanzprozessen
    li5: "KI-Agenten & Dokumentenverarbeitung",
    li6: "MCP & Systemintegration",
    li7: "Close- & Abstimmungsautomatisierung",
    li8: "Reporting-Pipelines",
    // Card 3: Individuelles Finanz-Ökosystem
    li9: "Individuelle Finanztools",
    li10: "ERP- & BI-Integration",
    li11: "Datenarchitektur",
    li12: "Machine-Learning-Modelle",

    // Expertise Descriptions
    expDesc1: "Ein im November verabschiedetes Budget ist im März meist überholt. Wir bauen treiberbasierte Rolling-Forecasts und Reporting, die mitlaufen, wenn sich das Geschäft ändert, damit der Plan auch später im Jahr noch etwas wert ist.",
    expDesc2: "In vielen Finanzteams werden Belegkontierung, Abstimmung und Monatsreporting noch von Hand gemacht. Wir verbinden die Systeme und automatisieren die wiederkehrenden Teile, und wir setzen KI-Agenten dort ein, wo etwas gelesen oder geprüft werden muss.",
    expDesc3: "Manchmal gibt es das passende Werkzeug noch nicht. Dann bauen wir es für Sie: Liquiditätstools, Preismodelle und die Datenbasis dahinter, passend dazu, wie Ihr Unternehmen arbeitet.",

    // Tech Section
    techTitle: "Technologie",
    techSub1: "Finanzplanung & Analytics",
    techSub2: "ERP & Integrationsplattformen",

    // Projects Section
    projectsTitle: "Projekte",
    p1Name: "Head of FP&A, Luftfahrt-Start-up",
    p1Desc: "Finanzmodellierung, Analyse von Lieferantenverträgen und makroökonomische Prognosen (z. B. Treibstoffpreise) mit SQL und Machine Learning. Eigene Finanztools gebaut, darunter Invoice OCR und eine Liquiditäts-Engine, integriert in die bestehenden Plattformen.",
    p2Name: "Integrierte Planung in Anaplan",
    p2Desc: "End-to-End-Anaplan-Implementierung für ein großes globales Healthcare-Unternehmen. Vertriebs- und Finanzplanung in einem treiberbasierten Modell zusammengeführt, sodass Vertriebsinputs direkt in die Finanzprognose einfließen.",
    p3Name: "",
    p3Desc: "",
    p4Name: "",
    p4Desc: "",
    seeStory: "Projekt ansehen →",

    // Contact Section
    contactSub: "Kontakt aufnehmen",
    contactTitle: "Termin vereinbaren",
    bookingKicker: "30-MINÜTIGES ERSTGESPRÄCH",
    bookingHeading: "Lassen Sie uns den Umfang klären",
    bookingIntro: "Ein kurzes Gespräch, um den Umfang zu verstehen — was nicht funktioniert, was Sie aufbauen möchten und ob wir zueinander passen. Keine Präsentation, keine Verpflichtung.",
    bookingBtn: "Verfügbare Zeiten anzeigen",
    bookingFallback: "Kalender lädt nicht? In neuem Tab öffnen →",
    bookingNote: "Öffnet Microsoft Bookings in einem neuen Tab. Es werden keine Daten an Microsoft übertragen, bevor Sie ihn öffnen — siehe unsere <a href=\"./privacy-policy.html\" target=\"_blank\" rel=\"noopener noreferrer\">Datenschutzerklärung</a>.",

    // Footer
    footer: "Copyright © 2026 PMG Services. Alle Rechte vorbehalten.",
    footerTagline: "Finanzielle Transformation & Digitalisierungsberatung",
    footerContactHeading: "Kontakt",
    footerLegalHeading: "Rechtliches",
    footerPrivacyLink: "Datenschutz",
    footerImpressumLink: "Impressum",

    // About Us Section
    auLabel: "Wer wir sind",
    auHeadline: "Sie haben die Daten.<br><span class=\"au-headline-accent\">Wir machen sie nutzbar.</span>",
    auLead: "Wenn Ihr Team die Daten hat, aber Mühe hat, daraus nützliche Erkenntnisse zu gewinnen, oder zu viel Zeit mit manueller Arbeit verbringt, dann kommen wir ins Spiel. Wir bauen die Forecast-Modelle, das Reporting und die Automatisierung, die Ihnen zeigen, wohin sich das Unternehmen entwickelt.",
    auFounderRole: "Gründer & CEO",
    auFounderBio: "Ich habe PMG Services gegründet, um die Lücke zwischen den Daten, die Finanzteams bereits haben, und den Entscheidungen, die sie tatsächlich treffen müssen, zu schließen. Zuvor habe ich bei einer Big-Four-Gesellschaft im Bereich Finance Transformation gearbeitet, und ich habe Teams von über 100 Personen in S/4HANA-Transformationsprojekten bei multinationalen Unternehmen geleitet. Wir bauen die Forecast-Modelle, das Reporting und die Automatisierung, die diese Daten nutzbar machen, und zwar so, dass sie auch ohne uns weiterlaufen.",
    founderCta: "Erstgespräch buchen",

    // How We Work Section
    hwwLabel: "Unser Prozess",
    hwwTitle: "Wie wir arbeiten",
    hwwIntro: "Drei Phasen mit einem klaren Enddatum. Sie wissen vorab, was Sie in jeder Phase bekommen und was es kostet.",
    hwwStep1Title: "Entdecken",
    hwwStep1Desc: "Wir schauen uns zuerst an, wie Ihr Finanzteam heute arbeitet: die Daten, die Prozesse, die Tools und die Tabellen, die nie jemand aufgeschrieben hat. Danach schicken wir Ihnen, was wir gefunden haben, bevor wir etwas vorschlagen.",
    hwwStep1Bullets: [
      "Prozess- & Entscheidungs-Audit",
      "Datenqualitätsbewertung",
      "Technologie-Landschaftsanalyse",
      "Stakeholder-Alignment"
    ],
    hwwStep2Title: "Gestalten",
    hwwStep2Desc: "Wir entwerfen die Lösung, von den Prozessregeln bis zur technischen Umsetzung. Jeder Teil davon ist an etwas Messbares geknüpft, und der Umfang steht fest, bevor jemand mit dem Bauen anfängt.",
    hwwStep2Bullets: [
      "Lösungsarchitektur",
      "KPI & Erfolgskennzahlen",
      "Implementierungs-Roadmap",
      "Risiko- & Change-Management"
    ],
    hwwStep3Title: "Umsetzen",
    hwwStep3Desc: "Wir bauen gemeinsam mit Ihrem Team und schulen es dabei, damit die Lösung weiterläuft, wenn wir weg sind.",
    hwwStep3Bullets: [
      "Agile Delivery-Sprints",
      "Integration & Testing",
      "Teamschulung & Adoption",
      "Hypercare & Übergabe"
    ],

    // Contact direct
    contactDirectIntro: "Lieber direkt Kontakt aufnehmen? Senden Sie uns eine E-Mail oder verbinden Sie sich auf LinkedIn."
  }
};

function setLanguage(lang) {
  const t = translations[lang];

  // Update Active State on Switcher
  document.querySelectorAll('.lang-link').forEach(link => {
    link.classList.remove('active');
    if (link.innerText.toLowerCase() === lang) link.classList.add('active');
  });

  // Navigation (desktop)
  document.getElementById('nav-aboutus').innerText  = t.navAbout;
  document.getElementById('nav-about').innerText    = t.navServices;
  document.getElementById('nav-projects').innerText = t.navProjects;
  document.getElementById('nav-contact').innerText  = t.navContact;

  // Hamburger Nav Links (About, Services, Projects, How We Work, FAQ, Contact)
  const mobileLinks = document.querySelectorAll('.menu-links a');
  if (mobileLinks.length >= 6) {
    mobileLinks[0].innerText = t.navAbout;
    mobileLinks[1].innerText = t.navServices;
    mobileLinks[2].innerText = t.navProjects;
    mobileLinks[3].innerText = t.navHowWeWork;
    mobileLinks[4].innerText = t.navFaq;
    mobileLinks[5].innerText = t.navContact;
  }

  // Hero Section
  document.querySelector('#profile .title').innerText = t.heroTitle;
  document.querySelector('.hero-description-text p').innerText = t.heroDesc;
  const heroBtnServices = document.querySelector('#profile .btn-color-2');
  if (heroBtnServices) heroBtnServices.innerText = t.btnServices;
  const heroBtnWork = document.querySelector('#profile .btn-color-1');
  if (heroBtnWork) heroBtnWork.innerText = t.btnWork;

  // Services Section — NEW CARD STRUCTURE
  document.querySelector('#about .title').innerText = t.servicesTitle;
  const cardTitles = document.querySelectorAll('.service-card-title');
  if (cardTitles.length >= 3) {
    cardTitles[0].innerText = t.colHeader1;
    cardTitles[1].innerText = t.colHeader2;
    cardTitles[2].innerText = t.colHeader3;
  }

  const cardDescs = document.querySelectorAll('.service-card-desc');
  if (cardDescs.length >= 3) {
    cardDescs[0].innerText = t.expDesc1;
    cardDescs[1].innerText = t.expDesc2;
    cardDescs[2].innerText = t.expDesc3;
  }

  const featureLists = document.querySelectorAll('.service-features');
  const features = [
    [t.li1, t.li2, t.li3, t.li4],
    [t.li5, t.li6, t.li7, t.li8],
    [t.li9, t.li10, t.li11, t.li12]
  ];
  featureLists.forEach((list, cardIndex) => {
    const items = list.querySelectorAll('li');
    items.forEach((li, i) => {
      if (features[cardIndex] && features[cardIndex][i]) {
        li.innerText = features[cardIndex][i];
      }
    });
  });

  // Projects
  document.querySelector('#projects .title').innerText = t.projectsTitle;
  const projCards = document.querySelectorAll('.project-card-future');
  const projKeys = [
    { name: 'p1Name', desc: 'p1Desc' },
    { name: 'p2Name', desc: 'p2Desc' },
    { name: 'p3Name', desc: 'p3Desc' },
    { name: 'p4Name', desc: 'p4Desc' }
  ];
  projCards.forEach((card, i) => {
    if (projKeys[i]) {
      const titleEl = card.querySelector('.project-title-future');
      const descEl = card.querySelector('.project-desc-future');
      const ctaEl = card.querySelector('.project-cta');
      if (titleEl) titleEl.innerText = t[projKeys[i].name];
      if (descEl) descEl.innerText = t[projKeys[i].desc];
      if (ctaEl) ctaEl.innerHTML = t.seeStory;
    }
  });

  // Contact
  const contactSubEl = document.querySelector('#contact .section__text__p1');
  if (contactSubEl) contactSubEl.innerText = t.contactSub;
  const contactTitleEl = document.querySelector('#contact .title');
  if (contactTitleEl) contactTitleEl.innerText = t.contactTitle;

  // Booking block
  const bk = {
    'booking-kicker': t.bookingKicker,
    'booking-heading': t.bookingHeading,
    'booking-intro': t.bookingIntro,
    'bookingLoadBtn': t.bookingBtn
  };
  Object.keys(bk).forEach(function (id) {
    const el = document.getElementById(id);
    if (el && bk[id]) el.innerText = bk[id];
  });
  const bookingNote = document.getElementById('booking-note');
  if (bookingNote) bookingNote.innerHTML = t.bookingNote;
  const bookingFallbackLink = document.getElementById('bookingFallbackLink');
  if (bookingFallbackLink && t.bookingFallback) bookingFallbackLink.innerText = t.bookingFallback;

  // Footer
  const footerCopyright = document.getElementById('footer-copyright');
  if (footerCopyright) footerCopyright.innerText = t.footer;
  const footerTagline = document.getElementById('footer-tagline');
  if (footerTagline) footerTagline.innerText = t.footerTagline;
  const footerContactHeading = document.getElementById('footer-contact-heading');
  if (footerContactHeading) footerContactHeading.innerText = t.footerContactHeading;
  const footerLegalHeading = document.getElementById('footer-legal-heading');
  if (footerLegalHeading) footerLegalHeading.innerText = t.footerLegalHeading;
  const footerPrivacyLink = document.getElementById('footer-privacy-link');
  if (footerPrivacyLink) footerPrivacyLink.innerText = t.footerPrivacyLink;
  const footerImpressumLink = document.getElementById('footer-impressum-link');
  if (footerImpressumLink) footerImpressumLink.innerText = t.footerImpressumLink;

  // Navigation — How We Work + FAQ
  const navHww = document.getElementById('nav-howwework');
  if (navHww) navHww.innerText = t.navHowWeWork;
  const navFaq = document.getElementById('nav-faq');
  if (navFaq) navFaq.innerText = t.navFaq;

  // About Us Section
  const auLabel = document.querySelector('#about-us .section-label');
  if (auLabel) auLabel.innerText = t.auLabel;
  const auHeadline = document.querySelector('.au-headline');
  if (auHeadline) auHeadline.innerHTML = t.auHeadline;
  const auLead = document.querySelector('.au-lead');
  if (auLead) auLead.innerText = t.auLead;
  const founderRole = document.getElementById('founder-role');
  if (founderRole) founderRole.innerText = t.auFounderRole;
  const founderBio = document.getElementById('founder-bio');
  if (founderBio) founderBio.innerText = t.auFounderBio;
  const founderCta = document.getElementById('founder-cta');
  if (founderCta) founderCta.innerText = t.founderCta;

  // How We Work Section
  const hwwLabel = document.querySelector('#how-we-work .section-label');
  if (hwwLabel) hwwLabel.innerText = t.hwwLabel;
  const hwwTitle = document.querySelector('#how-we-work .title');
  if (hwwTitle) hwwTitle.innerText = t.hwwTitle;
  const hwwIntro = document.querySelector('.hww-intro');
  if (hwwIntro) hwwIntro.innerText = t.hwwIntro;
  const hwwSteps = document.querySelectorAll('.hww-step');
  const hwwData = [
    { title: t.hwwStep1Title, desc: t.hwwStep1Desc, bullets: t.hwwStep1Bullets },
    { title: t.hwwStep2Title, desc: t.hwwStep2Desc, bullets: t.hwwStep2Bullets },
    { title: t.hwwStep3Title, desc: t.hwwStep3Desc, bullets: t.hwwStep3Bullets }
  ];
  hwwSteps.forEach((step, i) => {
    if (hwwData[i]) {
      const titleEl = step.querySelector('.hww-title');
      const descEl = step.querySelector('.hww-desc');
      const bullets = step.querySelectorAll('.hww-bullets li');
      if (titleEl) titleEl.innerText = hwwData[i].title;
      if (descEl) descEl.innerText = hwwData[i].desc;
      bullets.forEach((li, j) => {
        if (hwwData[i].bullets[j]) li.innerText = hwwData[i].bullets[j];
      });
    }
  });

  // Contact direct intro
  const contactDirectIntro = document.getElementById('contact-direct-intro');
  if (contactDirectIntro) contactDirectIntro.innerText = t.contactDirectIntro;

  // Keep the document language attribute in sync (SEO + screen readers)
  document.documentElement.lang = lang;
}

// --- Menu Toggle ---
function toggleMenu() {
  const menu = document.querySelector(".menu-links");
  const icon = document.querySelector(".hamburger-icon");
  menu.classList.toggle("open");
  icon.classList.toggle("open");
  // Update aria-expanded for accessibility
  const isOpen = menu.classList.contains("open");
  icon.setAttribute("aria-expanded", isOpen ? "true" : "false");
}

// Close mobile menu on outside click
document.addEventListener("click", function(e) {
  const menu = document.querySelector(".menu-links");
  const icon = document.querySelector(".hamburger-icon");
  if (menu && menu.classList.contains("open") &&
      !menu.contains(e.target) && !icon.contains(e.target)) {
    toggleMenu();
  }
});

// --- Nav Dark/Light Toggle on Scroll ---
(function() {
  const desktopNav = document.getElementById('desktop-nav');
  const hamburgerNav = document.getElementById('hamburger-nav');
  const hero = document.getElementById('profile');
  if (!hero) return;

  function updateNav() {
    const heroBottom = hero.offsetTop + hero.offsetHeight;
    const scrolled = window.scrollY > heroBottom - 80;
    if (desktopNav) desktopNav.classList.toggle('nav-scrolled', scrolled);
    if (hamburgerNav) hamburgerNav.classList.toggle('nav-scrolled', scrolled);
  }

  window.addEventListener('scroll', updateNav, { passive: true });
  updateNav();
})();

// --- Active Nav Section Tracking ---
(function() {
  const sections = document.querySelectorAll('section[id]');
  const navLinks = document.querySelectorAll('#desktop-nav .nav-links a');

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        const id = entry.target.getAttribute('id');
        navLinks.forEach(link => {
          link.classList.toggle('nav-active', link.getAttribute('href') === '#' + id);
        });
      }
    });
  }, { threshold: 0.3, rootMargin: '-20% 0px -50% 0px' });

  sections.forEach(section => observer.observe(section));
})();

// --- Intersection Observer for Card & Section Animations ---
(function() {
  const observerOptions = {
    threshold: 0.15,
    rootMargin: '0px 0px -60px 0px'
  };
  
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('animate-in');
      }
    });
  }, observerOptions);
  
  document.querySelectorAll('.service-card, .project-card-future, .details-container, .au-pillar, .hww-step, .qs-card, .faq-item, .problem-card').forEach(card => {
    observer.observe(card);
  });

  // Section header reveals
  const revealObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) entry.target.classList.add('visible');
    });
  }, { threshold: 0.1, rootMargin: '0px 0px -40px 0px' });

  document.querySelectorAll('.section-reveal').forEach(el => revealObserver.observe(el));
})();

// --- Smooth Section Navigation ---
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
  anchor.addEventListener('click', function (e) {
    const href = this.getAttribute('href');
    if (href === '#') return;
    e.preventDefault();
    const target = document.querySelector(href);
    if (target) {
      target.scrollIntoView({
        behavior: 'smooth',
        block: 'start'
      });
    }
  });
});

// --- Cursor Dot ---
(function() {
  if (window.matchMedia('(pointer: coarse)').matches) return;
  const dot = document.getElementById('cursor-dot');
  if (!dot) return;

  let mx = 0, my = 0, cx = 0, cy = 0;

  document.addEventListener('mousemove', e => { mx = e.clientX; my = e.clientY; });
  document.addEventListener('mouseleave', () => dot.classList.remove('cursor-visible'));
  document.addEventListener('mouseenter', () => dot.classList.add('cursor-visible'));

  const interactives = 'a, button, .service-card, .project-card-future, .faq-question, .problem-card';
  document.addEventListener('mouseover', e => {
    if (e.target.closest(interactives)) dot.classList.add('cursor-hover');
  });
  document.addEventListener('mouseout', e => {
    if (e.target.closest(interactives)) dot.classList.remove('cursor-hover');
  });

  dot.classList.add('cursor-visible');
  (function animate() {
    cx += (mx - cx) * 0.14;
    cy += (my - cy) * 0.14;
    dot.style.left = (cx - 3.5) + 'px';
    dot.style.top  = (cy - 3.5) + 'px';
    requestAnimationFrame(animate);
  })();
})();

// --- Back to Top Button ---
(function() {
  const btn = document.getElementById('back-to-top');
  if (!btn) return;

  window.addEventListener('scroll', function() {
    btn.classList.toggle('visible', window.scrollY > 400);
  }, { passive: true });

  btn.addEventListener('click', function() {
    window.scrollTo({ top: 0, behavior: 'smooth' });
  });
})();

// --- Booking calendar (click to load) ---
// The Microsoft Bookings iframe is not in the markup. It is created only when
// the visitor asks for it, so no connection to Microsoft is made -- and no IP
// address disclosed -- before they act. That click is the consent, which is why
// this needs no extra cookie-banner category.
(function () {
  const embed = document.getElementById('bookingEmbed');
  if (!embed) return;
  const btn = document.getElementById('bookingLoadBtn');
  const placeholder = document.getElementById('bookingPlaceholder');
  if (!btn || !placeholder || !embed.getAttribute('data-booking-url')) return;

  btn.addEventListener('click', function () {
    if (embed.querySelector('iframe')) return;

    // Read at click time so the URL can be swapped without reloading.
    const url = embed.getAttribute('data-booking-url');
    if (!url) return;

    const lang = (localStorage.getItem('pmg-lang') || 'en');

    // "Bookings with me" links (/bookwithme/) redirect to bookings.cloud.microsoft,
    // whose frame-ancestors policy refuses every external origin -- embedding one
    // yields a silent blank box. Those open in a new tab instead. Only a shared
    // Bookings page (Bookings app -> Share -> Embed) can actually be framed.
    if (url.indexOf('/bookwithme/') !== -1) {
      window.open(url, '_blank', 'noopener,noreferrer');
      if (window.pmgTrack) pmgTrack('generate_lead', { method: 'booking_calendar_newtab' });
      return;
    }
    const frame = document.createElement('iframe');
    frame.src = url;
    frame.className = 'booking-frame';
    frame.title = lang === 'de' ? 'Termin buchen' : 'Book a call';
    frame.loading = 'lazy';
    frame.setAttribute('referrerpolicy', 'no-referrer');
    frame.allow = 'clipboard-write';

    placeholder.remove();
    embed.classList.add('is-loaded');
    embed.appendChild(frame);

    // A cross-origin frame that refuses to be embedded fails silently -- the
    // visitor just gets a white box. Always surface a way out to a new tab.
    const fallback = document.getElementById('bookingFallback');
    const fallbackLink = document.getElementById('bookingFallbackLink');
    if (fallback && fallbackLink) {
      fallbackLink.href = url;
      fallback.hidden = false;
    }

    if (window.pmgTrack) pmgTrack('generate_lead', { method: 'booking_calendar_opened' });
  });
})();

// Duplicate observer for .details-container removed — handled by IIFE observer above.

// Apply translations on page load (?lang=de|en in the URL wins, so the
// hreflang alternate URLs actually serve the right language)
document.addEventListener('DOMContentLoaded', () => {
  const params = new URLSearchParams(window.location.search);
  const urlLang = params.get('lang');
  if (urlLang === 'de' || urlLang === 'en') {
    localStorage.setItem('pmg-lang', urlLang);
  }
  const lang = localStorage.getItem('pmg-lang') || 'en';
  setLanguage(lang);
});

// ═══════════════════════════════════════════════════
// INTERACTIVITY PACK — hero word reveal, hero particle
// network, industries marquee, card tilt, magnetic
// buttons, GA4 event tracking.
// ═══════════════════════════════════════════════════

const pmgReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
const pmgCoarsePointer = window.matchMedia('(pointer: coarse)').matches;

// --- Hero Particle Network (mouse-reactive) ---
(function () {
  if (pmgReducedMotion) return;
  const canvas = document.getElementById('hero-particles');
  const hero = document.getElementById('profile');
  if (!canvas || !hero) return;
  const ctx = canvas.getContext('2d');
  let W, H, particles = [], running = false, rafId = null;
  const mouse = { x: -9999, y: -9999 };

  function resize() {
    W = canvas.width = hero.offsetWidth;
    H = canvas.height = hero.offsetHeight;
  }

  function makeParticle() {
    return {
      x: Math.random() * W,
      y: Math.random() * H,
      vx: (Math.random() - 0.5) * 0.25,
      vy: (Math.random() - 0.5) * 0.25,
      r: Math.random() * 1.6 + 0.6,
      o: Math.random() * 0.25 + 0.08
    };
  }

  function init() {
    particles = [];
    const count = Math.min(Math.floor((W * H) / 16000), 90);
    for (let i = 0; i < count; i++) particles.push(makeParticle());
  }

  function step() {
    ctx.clearRect(0, 0, W, H);
    for (const p of particles) {
      p.x += p.vx;
      p.y += p.vy;
      if (p.x < 0 || p.x > W) p.vx *= -1;
      if (p.y < 0 || p.y > H) p.vy *= -1;

      // Gentle repulsion away from the cursor
      const dx = p.x - mouse.x, dy = p.y - mouse.y;
      const dist = Math.hypot(dx, dy);
      if (dist < 110 && dist > 0.001) {
        const push = (1 - dist / 110) * 0.6;
        p.x += (dx / dist) * push;
        p.y += (dy / dist) * push;
      }

      ctx.beginPath();
      ctx.arc(p.x, p.y, p.r, 0, Math.PI * 2);
      ctx.fillStyle = `rgba(255,255,255,${p.o})`;
      ctx.fill();
    }

    // Particle-to-particle links
    for (let i = 0; i < particles.length; i++) {
      for (let j = i + 1; j < particles.length; j++) {
        const a = particles[i], b = particles[j];
        const d = Math.hypot(a.x - b.x, a.y - b.y);
        if (d < 110) {
          ctx.beginPath();
          ctx.moveTo(a.x, a.y);
          ctx.lineTo(b.x, b.y);
          ctx.strokeStyle = `rgba(255,255,255,${0.05 * (1 - d / 110)})`;
          ctx.lineWidth = 0.5;
          ctx.stroke();
        }
      }
      // Red links to the cursor — the "interactive" signature
      const p = particles[i];
      const dm = Math.hypot(p.x - mouse.x, p.y - mouse.y);
      if (dm < 160) {
        ctx.beginPath();
        ctx.moveTo(p.x, p.y);
        ctx.lineTo(mouse.x, mouse.y);
        ctx.strokeStyle = `rgba(182,24,24,${0.35 * (1 - dm / 160)})`;
        ctx.lineWidth = 0.7;
        ctx.stroke();
      }
    }

    if (running) rafId = requestAnimationFrame(step);
  }

  // Only animate while the hero is on screen
  const visObserver = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting && !running) {
        running = true;
        rafId = requestAnimationFrame(step);
      } else if (!entry.isIntersecting && running) {
        running = false;
        if (rafId) cancelAnimationFrame(rafId);
      }
    });
  }, { threshold: 0.05 });
  visObserver.observe(hero);

  hero.addEventListener('mousemove', (e) => {
    const rect = canvas.getBoundingClientRect();
    mouse.x = e.clientX - rect.left;
    mouse.y = e.clientY - rect.top;
  });
  hero.addEventListener('mouseleave', () => { mouse.x = -9999; mouse.y = -9999; });

  window.addEventListener('resize', () => { resize(); init(); });
  resize();
  init();
})();

// --- Industries Infinite Marquee ---
(function () {
  const marquee = document.querySelector('.industries-marquee');
  const list = marquee && marquee.querySelector('.industries-list');
  if (!marquee || !list || pmgReducedMotion) return;
  // Duplicate the items once; translateX(-50%) loops seamlessly.
  const clone = document.createElement('div');
  clone.setAttribute('aria-hidden', 'true');
  clone.style.display = 'contents';
  clone.innerHTML = list.innerHTML;
  list.appendChild(clone);
  marquee.classList.add('marquee-on');
})();

// --- 3D Card Tilt + Glare ---
(function () {
  if (pmgCoarsePointer || pmgReducedMotion) return;
  const cards = document.querySelectorAll('.service-card, .project-card-future, .kpi-card, .qs-card');
  cards.forEach(card => {
    card.classList.add('tilt-card');
    let rx = 0, ry = 0, raf = null;

    card.addEventListener('pointermove', e => {
      const r = card.getBoundingClientRect();
      const px = (e.clientX - r.left) / r.width;
      const py = (e.clientY - r.top) / r.height;
      card.style.setProperty('--gx', (px * 100).toFixed(1) + '%');
      card.style.setProperty('--gy', (py * 100).toFixed(1) + '%');
      rx = (0.5 - py) * 5;
      ry = (px - 0.5) * 7;
      if (raf) return;
      raf = requestAnimationFrame(() => {
        card.style.transform = `perspective(900px) rotateX(${rx.toFixed(2)}deg) rotateY(${ry.toFixed(2)}deg) translateY(-4px)`;
        raf = null;
      });
    });
    card.addEventListener('pointerenter', () => card.classList.add('tilting'));
    card.addEventListener('pointerleave', () => {
      card.classList.remove('tilting');
      card.style.transform = '';
    });
  });
})();

// --- Magnetic Buttons (hero CTAs + form submit) ---
(function () {
  if (pmgCoarsePointer || pmgReducedMotion) return;
  document.querySelectorAll('#profile .btn-container .btn, .contact-submit-btn').forEach(btn => {
    btn.classList.add('btn-magnetic');
    btn.addEventListener('pointermove', e => {
      const r = btn.getBoundingClientRect();
      const dx = (e.clientX - (r.left + r.width / 2)) / r.width;
      const dy = (e.clientY - (r.top + r.height / 2)) / r.height;
      btn.style.transform = `translate(${(dx * 8).toFixed(1)}px, ${(dy * 6).toFixed(1)}px)`;
    });
    btn.addEventListener('pointerleave', () => { btn.style.transform = ''; });
  });
})();

// --- GA4 Event Tracking (no-ops until consent is granted — see consent.js) ---
(function () {
  const track = (name, params) => { if (window.pmgTrack) pmgTrack(name, params); };

  // Hero + section CTA clicks
  document.querySelectorAll('a[href="#contact"]').forEach(a => {
    a.addEventListener('click', () => track('cta_click', {
      cta_text: a.textContent.trim().slice(0, 60),
      cta_location: (a.closest('section') || {}).id || 'nav'
    }));
  });

  // Booking calendar open (the booking IIFE also fires its own event)
  const bookingBtn = document.getElementById('bookingLoadBtn');
  if (bookingBtn) bookingBtn.addEventListener('click', () => track('booking_calendar_open', {}));

  // FAQ engagement
  document.querySelectorAll('.faq-item').forEach(d => {
    d.addEventListener('toggle', () => {
      if (d.open) {
        const q = d.querySelector('.faq-question');
        track('faq_open', { question: q ? q.textContent.trim().slice(0, 80) : '' });
      }
    });
  });

  // Case study opens
  document.querySelectorAll('.project-card-future').forEach(c => {
    c.addEventListener('click', () => {
      const t = c.querySelector('.project-title-future');
      track('case_study_open', { title: t ? t.textContent.trim() : '' });
    });
  });

  // Language switch
  document.querySelectorAll('.lang-link').forEach(b => {
    b.addEventListener('click', () => track('language_switch', { language: b.textContent.trim() }));
  });

  // Outbound: email + LinkedIn
  document.querySelectorAll('a[href^="mailto:"]').forEach(a =>
    a.addEventListener('click', () => track('email_click', { location: (a.closest('footer') ? 'footer' : 'contact') })));
  document.querySelectorAll('a[href*="linkedin.com"]').forEach(a =>
    a.addEventListener('click', () => track('linkedin_click', { location: (a.closest('footer') ? 'footer' : 'contact') })));

  // Scroll depth: 25 / 50 / 75 / 90 %
  const marks = [25, 50, 75, 90];
  const fired = {};
  window.addEventListener('scroll', () => {
    const pct = ((window.scrollY + window.innerHeight) / document.documentElement.scrollHeight) * 100;
    marks.forEach(m => {
      if (pct >= m && !fired[m]) {
        fired[m] = true;
        track('scroll_depth', { percent: m });
      }
    });
  }, { passive: true });
})();
